{# The shape summary.md follows. Rendered by reports/summary.py, which builds
   the document in code rather than substituting into this file: a template
   engine would make the report's structure depend on a dependency, and this
   file exists so a reader can see the intended shape in one place. #}

# {experiment_name} — summary

Run `{run_id}` · rendered {rendered_at}

```yaml
{health_vector}
```

## What this run can and cannot say
## Does a Pheasant-only agent rival the specialist?
## Headline comparisons
## Limitations of this run
## Collection
## Where to look next
